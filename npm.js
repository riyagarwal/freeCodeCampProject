 /* npm - global command- comes with node
    npm --version

    global dependency - such a package can be used in any project(i.e. the package will be available globally)
    npm install -g <packagename>

    local dependency- such a package can be used only in the package in which it is installed
    npm i packageName */

    /* to install package.json file, use the command- npm init -y
       package.json stores imp. info about project/package */

    //    we have installed bootstrap only for understanding purpose. Also installing an external package creates a folder called node_modules
    